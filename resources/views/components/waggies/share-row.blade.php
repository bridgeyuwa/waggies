@props(['title', 'description' => '', 'contextLabel' => 'Share this service'])
<div x-data="waggiesShare('{{ addslashes($title) }}', '{{ addslashes($description) }}')" role="group" aria-label="{{ $contextLabel }}" class="flex flex-col gap-3 rounded-2xl bg-surface-purple/60 px-4 py-6 sm:flex-row sm:items-center sm:gap-4 md:px-6"><span class="shrink-0 text-sm font-semibold text-primary-dark">{{ $contextLabel }} :</span><div class="flex flex-wrap items-center gap-2">@foreach([['Copy link to clipboard','link'],['Share on WhatsApp','whatsapp'],['Share on Facebook','facebook'],['Share on X / Twitter','x'],['Share on LinkedIn','linkedin'],['Share via email','email']] as [$label,$icon])<button type="button" aria-label="{{ $label }}" @click="share('{{ $icon }}')" :aria-label="copied && '{{ $icon }}' === 'link' ? 'Link copied to clipboard' : '{{ $label }}'" class="flex h-11 w-11 items-center justify-center rounded-full border border-primary/40 bg-white text-primary-dark/70 transition-colors duration-200 hover:border-primary hover:bg-primary hover:text-white" x-html="copied && '{{ $icon }}' === 'link' ? checkIcon : icon('{{ $icon }}')"></button>@endforeach</div></div>
